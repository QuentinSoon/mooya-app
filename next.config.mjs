/** @type {import('next').NextConfig} */
const nextConfig = {
	// async redirects() {
	// 	return [
	// 		{
	// 			source: '/:path*',
	// 			has: [{ type: 'host', value: 'www.mooya.fr' }],
	// 			destination: 'https://mooya.fr/:path*',
	// 			permanent: true,
	// 		},
	// 		// If you need more redirects, they can be added JS objects here.
	// 	];
	// },
};

export default nextConfig;
